
const manageRoundSocket = (socket, db, io) => {

  // socket.on('submitAnswer', (gameRoom) => {
  //   // this will need to be user specific
  //   io.in(gameRoom).emit('transitionAwait');
  // });

};

module.exports = { manageRoundSocket };
